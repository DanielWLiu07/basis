# Build output
/build/
/build-*/
/out/
cmake-build-*/

# Compile DB symlink (generated)
/compile_commands.json

# Recorded feed captures and benchmark scratch (large, regenerated)
/captures/
*.feedlog

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
.cache/

# Local secrets: API keys for Kalshi/Polymarket never get committed
.env
*.local.toml
secrets/

# Key material, wherever it lands (secrets/ above covers the path;
# these cover a key pasted somewhere else by accident)
*.pem
*.key
*.p12
*.pfx
